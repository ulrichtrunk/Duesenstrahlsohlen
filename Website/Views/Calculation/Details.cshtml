@using Website.Models
@model CalculationViewModel
@{
    ViewBag.Title = "Calculation Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-12">
        <section>
            <div id="formContainer">
                @{ Html.RenderPartial("Form", Model); }
            </div>
        </section>
    </div>
</div>